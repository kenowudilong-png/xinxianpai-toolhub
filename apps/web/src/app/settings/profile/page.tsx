export const dynamic="force-dynamic";import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import {AppShell} from "@/lib/layout";export default async function Profile(){const user=await currentUser();if(!user)redirect("/login");return <AppShell user={user} title="个人设置"><section className="rounded-3xl border bg-card p-6"><h1 className="text-2xl font-semibold">个人设置</h1><p className="mt-4">用户名：{user.username}</p><p>姓名：{user.displayName}</p><p>角色：{user.role==="admin"?"管理员":"成员"}</p></section></AppShell>}

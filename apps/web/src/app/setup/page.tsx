export const dynamic="force-dynamic";
import Image from "next/image";import {redirect} from "next/navigation";import {brand} from "@/lib/brand";import {isInitialized} from "@/lib/auth";import {setupAction} from "@/lib/actions";import {PasswordField,SubmitButton} from "@/components/client-controls";
export default function SetupPage(){if(isInitialized())redirect("/login");return <main className="grid min-h-screen place-items-center p-6"><section className="w-full max-w-md rounded-3xl border bg-card p-8 shadow-xl"><Image src={brand.logo} alt={brand.name} width={260} height={96} className="mx-auto mb-4 h-auto w-64 object-contain"/><h1 className="text-center text-2xl font-semibold">初始化工具站</h1><form action={setupAction} className="mt-6 space-y-4"><label className="block text-sm">管理员用户名<input className="mt-2 w-full rounded-xl border bg-background px-3 py-2" name="username" required/></label><label className="block text-sm">管理员密码<div className="mt-2"><PasswordField/></div></label><SubmitButton>创建管理员</SubmitButton></form></section></main>}
